<?php 
	// $globals : globals settings
    // $actual_srv = params defined by system

    if(isset($actual_srv["key"]) && strlen($actual_srv["key"])) {
/*    	if(check_function("set_field_gmap")) { 
			set_field_gmap();
		} 
*/  }
